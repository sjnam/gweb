# build outputs
/bin/
/.bootstrap/
/build/

# tangled / woven artifacts
*.go.tmp
examples/*.go
examples/*.tex
examples/*.dvi
examples/*.pdf
examples/*.ps
examples/*.log
examples/*.toc
examples/*.idx
examples/*.scn

# but keep sources
!examples/*.w

# rasterized previews
*.png

# stray TeX scratch logs, and CWEB cweave aux files (.idx index, .scn section
# names) -- gweave never writes these, but running cweave in-tree drops them
texput.log
*.toc
*.idx
*.scn

# VS Code extension package
*.vsix
